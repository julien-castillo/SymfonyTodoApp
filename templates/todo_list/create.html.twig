{% extends "base.html.twig" %}

{% block body %}
   <div class="container">
      {{ form_start(form) }}
          {{ form_row(form.name) }}
          {{ form_row(form.color) }}
          <input type="submit" value="Créer" class="btn btn-success mt-2">
      {{ form_end(form) }}
   </div>
{% endblock %}